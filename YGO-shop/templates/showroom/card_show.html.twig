{% extends 'base.html.twig' %}

{% block title %}Card{% endblock %}

{% block main %}
    <h1>Card</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ card.id }}</td>
            </tr>
            <tr>
                <th>Card_name</th>
                <td>{{ card.cardName }}</td>
            </tr>
            <tr>
                <th>Card Categories</th>
                <td>{{ card.Categories|join(', ') }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_showroom_show', { 'id': showroom.id}) }}">back to The showroom</a>

    <a href="{{ path('app_card_edit', {'id': card.id}) }}">edit</a>

    {{ include('card/_delete_form.html.twig') }}
{% endblock %}
